<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" />
    <corners android:radius="16dp" /> <!-- Smaller radius for a small circle -->
    <stroke android:width="1dp" android:color="@color/black" /> <!-- The stroke around the circle -->
</shape>
